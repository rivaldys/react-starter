import React from 'react'
import { Layout } from '../../components'

const Home = () =>
{
    return (
        <Layout>
            <p className="App-desc">You're on <span>Home Page</span></p>
            <p className="App-desc">Edit <code>src/pages/Home/index.js</code> and save to reload.</p>
        </Layout>
    )
}

export default Home